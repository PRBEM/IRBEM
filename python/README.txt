INSTALL 

Steps to install the Python2/3 wrapper for IRBEM:
1. follow the instalation instructions at ./../README.install
   to compile the source code. 
2. On unix systems, run the command 'sudo python3 setup.py install'.
   This will add variables to PYTHONPATH which will allow the
   Python wrapper to be called from any working directory.
